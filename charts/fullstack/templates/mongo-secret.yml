apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.mongo-secret.name }}
  namespace: {{ .Values.namespace }}
type: {{ .Values.mongo-secret.type }}
stringData:
  {{- range $key, $value := .Values.mongo-secret.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}