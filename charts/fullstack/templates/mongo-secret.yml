{{- $mongo := index .Values "mongo-secret" }}

apiVersion: v1
kind: Secret
metadata:
  name: {{ $mongo.name }}
  namespace: {{ .Values.namespace }}
type: {{ $mongo.type }}
stringData:
  {{- range $key, $value := $mongo.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}