{{- $name := .Values.frontend-deployment.name }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $name }}
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.frontend-deployment.replicaCount }}
  selector:
    matchLabels:
      app: {{ $name }}
  template:
    metadata:
      labels:
        app: {{ $name }}
    spec:
      containers:
      - name: {{ $name }}
        image: {{ .Values.frontend-deployment.image.repository }}
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: {{ .Values.frontend-deployment.resources.requests.cpu }}
            memory: {{ .Values.frontend-deployment.resources.requests.memory }}
          limits:
            cpu: {{ .Values.frontend-deployment.resources.limits.cpu }}
            memory: {{  .Values.frontend-deployment.resources.limits.memory }}
        envFrom:
        - secretRef:
            name: {{ .Values.frontend-deployment.secretName.frontend }}