{{- $frontend := index .Values "frontend-service" }}

apiVersion: v1
kind: Service
metadata:
  name: {{ $frontend.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ $frontend.selector.app }}
  ports:
    - port: {{ $frontend.spec.port }}
      targetPort: {{ $frontend.spec.targetPort }}